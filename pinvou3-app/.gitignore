# Tauri 后端构建产物
src-tauri/target/
# Note: src-tauri/Cargo.lock 是 binary crate 应该 commit 的（锁依赖版本）

# 前端依赖（@tauri-apps/cli native binary 等）
node_modules/
# 例外：三省六部「网页类」预置模板的依赖必须进 git——客户机沙箱断网装不了,
# 靠这份预装 node_modules 离线 `npm run build` 出单文件(见 src-tauri/tauri.conf resources）。
# 两条覆盖顶层 + 嵌套(如 micromatch/node_modules)。
!src-tauri/resources/web-template/node_modules/
!src-tauri/resources/web-template/**/node_modules/

# Tauri 生成的快照
src-tauri/gen/

# OS
.DS_Store
Thumbs.db
