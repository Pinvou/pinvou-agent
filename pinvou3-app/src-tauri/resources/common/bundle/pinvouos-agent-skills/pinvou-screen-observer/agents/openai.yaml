interface:
  display_name: "PinvouOS Screen Observer Agent"
  short_description: "Observe accessible screen facts without guessing"
  default_prompt: "Use $pinvou-screen-observer to inspect what PinvouOS can reliably observe on screen."
