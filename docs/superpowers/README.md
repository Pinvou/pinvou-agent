# superpowers — agent 辅助开发过程留档

本目录存放 agent 辅助开发过程的留档，供回溯开发脉络与设计推理使用：

- `plans/`：按任务分解记录的实施计划。
- `specs/`：配套设计稿。

命名规则：`YYYY-MM-DD-主题.md`，日期为写作时点。

这些文档是**历史快照**：文中引用的路径、文件结构与交互细节均以写作时点为准，可能已被后续演进取代；现行实现一律以源码为准。

与 [`docs/adr/`](../adr/) 的分工：`adr/` 记录架构决策的结论，本目录记录实施过程与设计推理。
