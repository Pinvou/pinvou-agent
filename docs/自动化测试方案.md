# pinvou3 自动化测试方案

> 更新 2026-07-20 · WebUI v2 本地验证已落地 · 正式 Web dist 发布流程尚未启用

不依赖真模型的用户旅程入口为 `scripts/run-user-journey-tests.sh`，默认覆盖 Markdown、
session replay auditor 单测、MCP 协议/产物契约、桌面/Web Vite 构建、通用 Mock GUI、工具商店
装卸/授权、知识库 Mock GUI、WebUI Relay v2 协议与完整 WebUI 浏览器旅程；还可附加真实 session replay audit 与 L1 真模型 harness。完整覆盖矩阵见
`docs/真实用户自动化测试矩阵.md`。

## 验证分层

```
L2 Backend 确定性测试 (cargo test --lib)           ← PR gate
Mock GUI smoke (Chromium + mock Tauri)             ← 本机 / 可进 CI
MCP contract (真实 stdio + 临时目录产物)            ← 本机 / PR gate
完整 WebUI smoke (真实 relay + 共享 UI + WS 桌面模拟)  ← 本机 / CI
Session replay audit (真实 session JSON)           ← 本机 / 可进 CI
L1 真 vLLM dialog harness (cargo test --ignored)   ← 本地手跑 + 落 transcript
Judge 跨模型离线评 Qwen 答案质量                   ← 读 transcript 写报告
```

**L1 跟 cargo test PASS/FAIL 解耦**:L1 只断 judge 看不见的硬指标 (`files_exist` 文件落盘 / `max_duration_s` 耗时上限);行为质量评判全部委托 Judge。

## L2 — Backend 确定性测试

- 位置: `pinvou3-app/src-tauri/src/{commands,bridge/mod,file_ingest,lib}.rs::tests`
- 覆盖: `validate_user_path` 边界 / `pinvou3_blocklist` 契约 / bridge mode 字段 / `{{PINVOU3_WORKSPACE}}` 占位符 / file_ingest dispatch
- 跑: `cargo test --manifest-path pinvou3-app/src-tauri/Cargo.toml --lib -- --test-threads=1`，每 PR 自动跑
- 数量持续增长，以 `cargo test ... -- --list` 输出为准，不在文档写易过期的固定数字

### Windows 本机 L2 风险记录

2026-07-21 在 Windows 本机验证时，`cargo test --manifest-path pinvou3-app/src-tauri/Cargo.toml --lib -- --test-threads=1`
能完成编译，但测试可执行文件启动前退出，错误为 `STATUS_ENTRYPOINT_NOT_FOUND`。该失败发生在
Rust test harness 进入用例前，不是具体单测断言失败。

已排除的方向：

- 默认构建中 `fastembed -> ort/ort-sys` 会静态链接 ONNX Runtime，并引入 DirectML/D3D12/DXCore。
- `--no-default-features` 依赖图已不包含 `fastembed` / `ort`，`cargo check --lib --no-default-features` 通过。
- 但 `cargo test --lib --no-default-features` 的测试 exe 仍以 `STATUS_ENTRYPOINT_NOT_FOUND` 退出；直接导入表也未发现缺失符号。

因此本机目前不能把 L2 Rust lib test 记为通过；需要继续查 Windows loader / native runtime / Tauri 测试目标依赖。前端和用户旅程 smoke 通过不能替代该项。

## Mock GUI + Session replay

- `pinvou3-app/tests/ui_smoke.js`：启动恢复、工具商店、产物卡、品悟、模式 chip、本地模型引导
- `pinvou3-app/tests/settings_ui_smoke.js`：设置页模型/搜索/权限的全局配置回归；覆盖 iOS 列表交互、删除二次确认、搜索源保存时机、凭据输入和权限失败 toast
- `pinvou3-app/tests/kb_smoke.js`：本地文件、加入知识库、知识集聚焦、添加文件参数透传
- `pinvou3-app/tests/tool_store_smoke.js`：6 个 MCP 装卸、Obsidian 探测、配套/独立技能及飞书/企微/钉钉/EIP/知道 5 类授权连接器
- `scripts/mcp-server-contract-smoke.py`：真实 stdio MCP 协议、Obsidian 读写、PPTX/公文产物及 QCC 清单
- `remote-control-relay/test/*.test.js`：WebUI Relay v2 endpoint 鉴权、单客户端 lease、盲转发、桌面离线/重连、限流、安全与静态站点行为
- `remote-control-relay/test/web-ui.smoke.cjs`：真实共享 WebUI 端到端旅程（真实本地 Relay，WebSocket 模拟 desktop endpoint v2）
- `scripts/session-replay-audit.py`：真实 transcript 结构、工具调用配对、用户询问、产物引用
- `scripts/tests/test_session_replay_audit.py`：审计器自身的确定性回归
- 默认入口会按需安装 app/relay 依赖，分别构建桌面 `dist/` 和 Relay 使用的 `web/dist/`，并在 Windows/macOS/Linux 常见路径自动探测 Chrome/Chromium；缺 Chromium / puppeteer 时 GUI smoke 以 exit 2 跳过，但发版前不得把关键 GUI 项长期停留在 skip。

### 设置页全局自动化测试

本轮设置页重构把通用、模型、搜索、记忆等场景重新分组，模型与搜索源统一为列表式交互。高风险点单独用
`pinvou3-app/tests/settings_ui_smoke.js` 锁定：

- 模型：本地模型显示“本地模型”标签且不可删除；默认模型用左侧圆圈选择；删除模型必须先出现 270px 级 iOS 确认框；新增云模型不显示无效 API 地址，API Key 为空时保存置灰。
- 编辑模型：云模型编辑时保留并展示用户已保存的 API Key 状态；默认直接显示掩码点位，不显示“已配置”这类技术状态文案。点“显示”后仅对后端已保存的密钥做明文回显；环境变量覆盖的 Key 不回显明文，只保持掩码状态。模型 ID、API Key、API 地址等输入框必须连续输入不丢焦点。编辑已有云模型时，“更换”只允许切换同一厂商下的其他模型，因为同厂商 Key 通常通用；`OpenAI Compatible` 这类自定义兼容端点不跨端点复用 Key，不同 base URL 走新增模型。
- 搜索：不再出现“保存搜索配置”按钮；新增搜索源先进入编辑弹窗，未点击弹窗“保存”不得写入；已有搜索源未输入新 Key 时“保存”置灰，只有新增 Bing 或输入新 Key 后才能保存；点击保存后只显示重启确认，点“现在重启”才调用 `save_settings_and_restart`。
- 权限：高级执行权限打开失败时回滚开关，并用 toast 显示错误，不进入设置页错误边界。
- 帮助反馈：反馈弹窗按模型/搜索弹窗统一规格实现，宽度 430px 级、最大宽度不超过 viewport、22px 圆角、紧凑 header、单层列表式表单组和底部操作区；不得使用 520px 级大弹窗、过大标题、卡片套卡片或系统原生 `alert` 成功提示。
- 任务收纳联动：收纳成功 toast 的“前往查看”必须打开设置页并定位到“数据管理”，收纳记录在列表中可见；toast 按钮文本不得折行。
- 响应式布局：设置弹窗必须随 PINVOU 主窗口宽高动态收缩，侧栏和内容区不得撑出 viewport；通用页的“界面语言”等短标签不能被压成逐字竖排。聊天首页欢迎语在小窗口下必须换行或降到小号断点，不得产生横向溢出。

本地最小回归命令：

```bash
cd pinvou3-app
npm run build:ui
npm run test:settings-ui
```

更完整的设置页风险回归走统一入口：

```bash
bash scripts/run-user-journey-tests.sh
```

该入口必须实际运行 `settings_ui_smoke.js`；若旧 GUI smoke 因本机缺浏览器被 skip，需要在结果中明确记录 skip 原因，不能等同于完整通过。

### 模型目录刷新方案

2026-07-21 已按厂商官方文档核对并刷新前端 `MODEL_CATALOG`，当前发版候选以内置列表和自定义入口共同覆盖可接入模型。更新原则：

- 内置列表只放各厂商当前主推、仍可新接入、适合文本/Agent/代码对话的模型；旧模型不从已有用户配置里删除，但不再出现在“添加/更换”推荐列表。
- 每个厂商仍保留“自定义模型 ID”，避免官方灰度、地域差异、企业专属模型无法接入。
- 编辑已有云模型时只展示同一厂商的可选模型；跨厂商或 OpenAI-compatible 不同 base URL 走新增模型。
- `OpenAI Compatible` 不是 OpenAI 官方模型目录，内置项仅作为兼容端点示例；无法确认真实可用时优先使用“自定义兼容模型”。
- 官方文档无法给出稳定公开模型 ID 的厂商，不臆造完整列表，只放官方明确的通用/Agent 模型，并保留自定义入口。

当前内置列表：

| 厂商 | 旧版问题 | 当前内置项 |
|---|---|---|
| DeepSeek | `deepseek-chat` 是兼容旧名，官方标注 2026-07-24 后弃用；`deepseek-v4-pro/flash` 可保留 | `deepseek-v4-pro`、`deepseek-v4-flash` |
| Kimi | 缺 `kimi-k3`；`kimi-k2.5` 和 Moonshot V1 系列停止向新注册用户开放并计划下线 | `kimi-k3`、`kimi-k2.7-code`、`kimi-k2.7-code-highspeed`、`kimi-k2.6` |
| GLM | 缺最新 `glm-5.2`；`glm-4-plus` 已不应作为推荐默认 | `glm-5.2`、`glm-5-turbo`、`glm-4.7`，可选保留 `glm-5.1` |
| MiniMax | `minimax-m1`/`abab6.5s-chat` 已不是官方当前语言模型推荐 | `MiniMax-M3`、`MiniMax-M2.7`、`MiniMax-M2.7-highspeed`、`MiniMax-M2.5`、`MiniMax-M2.5-highspeed` |
| MiMo | `mimo-v2-flash` 官方已提示 V2 系列 2026-06-30 下线 | `mimo-v2.5-pro`、`mimo-v2.5`；`mimo-v2.5-pro-ultraspeed` 标注“内测/灰度” |
| 通义千问 | `qwen-max/qwen-plus/qwen-turbo` 不是当前推荐列表 | `qwen3.7-plus`、`qwen3.6-flash` |
| 豆包 | `doubao-pro-256k` 已落后于 Seed 系列 | `doubao-seed-evolving`、`doubao-seed-2.1-pro`、`doubao-seed-2.1-turbo`、`doubao-seed-2.0-pro`、`doubao-seed-2.0-lite` |
| OpenAI Compatible | 旧版写死 `gpt-4o` 容易过期；该入口本质是兼容端点 | 以“自定义兼容模型”为主；当前保留 `gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.6-sol` 示例项，并保留用户手填 |

自动化覆盖：

- `settings_ui_smoke.js` 断言新增/编辑模型的推荐列表不出现官方已下线或标注弃用的模型名，并验证多个厂商代表模型存在。
- 保存链路当前覆盖 DeepSeek 内置模型和 DeepSeek 自定义模型 ID 连续输入；自定义模型入口仍可手填任意模型 ID。
- 逐厂商保存链路尚未展开成矩阵测试，若后续每个厂商出现不同字段或校验规则，需要把每个厂商至少一个内置项纳入保存回归。
- 不把模型目录写成“永久正确”；每次发版前需要按官方文档重新核对。

## 完整 WebUI 覆盖边界

自动化覆盖三层：Relay v2 协议与安全、真实共享 React WebUI 的浏览器边界、桌面 Rust
WebAccessManager 的确定性边界。浏览器旅程先执行 `build:web`，再由真实 Relay 提供静态资源；
同一份页面分别以 1440×900 电脑 viewport 和 390×844 手机 viewport 加载。测试用
WebSocket 模拟持久 desktop endpoint，给页面启动阶段的 allowlisted RPC 返回最小有效值，
并验证 fragment 中的 endpoint/token 不进入 HTTP URL、唯一 Web lease 接管、至少一次带
`client_request_id` 的 RPC 往返，以及事件 `stream_epoch + seq` 写入游标后在重连时通过
`after_seq` 续传。浏览器运行时错误和手机横向溢出也会直接使测试失败。

Rust 确定性测试负责 RPC 去重缓存、事件 epoch/单调序号/有界 journal、重放与 reset、
endpoint 凭据持久化和撤销、host file picker 范围及 Session 产物下载路径边界。Relay 测试
负责 token/secret hash、鉴权时限、消息类型白名单、旧 lease 隔离、桌面离线恢复、容量/限流、
healthz 脱敏和 SPA 缓存头。未自动化的是 Firefox/iOS Safari/Android Chrome 的浏览器差异、
真机触屏键盘与安全区、弱网切换、公网 TLS/反向代理及真 Tauri 桌面事件桥；这些保留为
发版前人工或线上 smoke。

`build:web` 产生 `remote-control-relay/web/dist/`。测试端点脚本以 `/pinvou3/remote-test`
重建并发布，正式脚本以 `/pinvou3/remote` 重建、整体备份/替换并验证公网；smoke 通过只说明
产物可测，仍须经过发布审批并显式设置 `PINVOU_CONFIRM_PRODUCTION_DEPLOY=1` 才能上线。

## L1 — 真模型 scenario harness

- 位置: `pinvou3-app/src-tauri/tests/l1_dialog_harness.rs`
- 跑: `PINVOU3_L1_REQUIRE_VLLM=1 cargo test --test l1_dialog_harness -- --ignored --test-threads=1`（严格模式，端点掉线或 turn 内 Engine Error 直接失败）
- 健康探针: vLLM `/v1/models` 3s 探活；普通开发模式不在线可 skip，严格验收模式必须失败
- Auto-approve / auto-cancel-user-input: harness 内置,模拟生产 event_forwarder

### Scenario 清单

| 分类 | scenario | 防什么 |
|---|---|---|
| 基础 | `translate_no_tool` | INSTRUCTIONS_MD 过激探目录 |
| 基础 | `reasoning_off_speed` | thinking 关 + 单 turn <15s |
| 工具链 | `batch_create_7_files` | OpenAI streaming batch tool_calls bug (PR #1686) |
| 工具链 | `save_to_tmp_no_validate_fail` | validate_user_path A 方案放宽 |
| 工具链 | `relpath_write_file` | 相对路径落到会话 workspace |
| 工具链 | `data_analysis_csv` | read_file + code_execution 链式 |
| 工具链 | `write_okr_md` | write_file 结构化产物 |
| 多 turn | `multi_turn_context` | 上下文连贯性 (2 turn) |
| Plan | `plan_mode_list_dir` | trust_mode=true 跨 workspace |
| Plan | `plan_pure_planning` | 纯规划不越权执行 |
| Plan | `plan_dev_task_plan` | 开发任务计划质量 |
| Plan | `plan_mode_write_blocked` | Plan 阶段写入硬拦截 |
| Plan | `plan_travel_web` | web_search 多步 + update_plan |
| YOLO | `yolo_large_html` | 大型网页产物生成与落盘 |
| 权限 | `sudo_off_root_task` | sudo 关闭时不执行提权任务 |
| 边界 | `refusal_correct` | 安全敏感请求拒答正确性 |
| 边界 | `long_output_1500` | 长文档输出能力 |
| 边界 | `chinese_idiomatic` | 中文表达 + 受众适配 |
| 边界 | `tool_error_recovery` | 工具失败优雅 recover |
| **subagent** | `subagent_single_simple` | 单 subagent 链路诊断 |
| **subagent** | `subagent_no_need` | 简单任务不滥用 subagent (反向 case) |
| **subagent** | `subagent_compare_3_libs` | 任务拆分 + 多 subagent 并行 + 结果综合 (核心场景) |
| **subagent** | `subagent_research_topic` | 发散研究方向拆分 (用户实际场景) |
| **subagent** | `subagent_one_fails` | subagent 失败后主 agent 优雅 recover |
| 多模态 | `image_vision_analyze` | 图片输入、视觉工具与结果综合 |
| 大附件 | `large_xlsx_attachment_path_mode` | 大型表格走路径模式避免上下文爆炸 |
| sanity | `l1_health_and_boot` | harness 框架自身 ready |

### Subagent scenarios 启用机制

subagent 类工具默认在 L1.5 blocklist 内 (生产屏蔽)。L1 scenario 用 `SubagentEnv::enable()` RAII guard 临时 set `PINVOU3_BLOCKLIST_OVERRIDE` env var 解锁,scenario 结束自动复原。fork patch `pinvou3_blocklist::is_pinvou3_hidden` 检查此 env (commit `1ba8e41`)。

### 必需的 fork plumbing (已落地)

- `Pinvou3Bridge::boot_with_workspace(ws: PathBuf)` — scenario tempdir 隔离
- `AppEngine::spawn_headless(bridge)` — 不要 Tauri AppHandle / 不启 event forwarder

## Judge — Claude 跨模型评 Qwen

详 `docs/L1-judge-rubric.md` (当前 rubric **r2**,加 subagent 评估 2 维)。

**核心机制**:
- Qwen 跑 → harness 落 transcript md → Claude 读 → 写 judge report → ≤3 离群点 auto-append `process.md`
- 6 维 × 1-5 分: 准确性 / 完整性 / 简洁性 / 工具使用合理性 / **任务拆分合理性 (r2 新)** / **结果综合能力 (r2 新)** (N/A 跳过)
- 后 2 维只对 subagent scenario 评 (非 subagent 永远 N/A)
- **跟 L1 cargo test 解耦** — judge 漂移不阻塞 PR

**Baseline 多维命名** (`docs/l1-baselines/v<app_ver>[-<vllm_tag>]-r<rubric_ver>[-<suffix>]/`):
- `<app_ver>` 代码版本 + INSTRUCTIONS_MD
- `<vllm_tag>` 可选,标 vLLM 配置变更 (例 `vllm2`)
- `<rubric_ver>` 评分尺子版本
- `<suffix>` scenario 集合大小 (例 `13scn` / `17scn`)
- 跨 rubric diff 拒绝;跨 vllm 配置可 diff 但需意识底座变了
- 已有: `v0.8.37-r1` (5 scn,4.75) / `v0.8.37-r1-13scn` (13 scn,4.67) / `v0.8.37-vllm2-r2-17scn` (17 scn,4.46;含 4 subagent + vLLM 优化)

**离群点闭环**:
- 任一维度 ≤3 → judge report 列出 + auto-append `process.md` `## L1 judge 离群点跟进`
- 重复出现 grep 同 scenario+维度,改状态行为 🔁
- 状态: 🆕 / 🔁 / ✅ / 🧪 / ❌

**Sanity 协议**:
- 同 session retake 是"演习"不是考试
- 真 sanity 要"开新 session 让 Claude 不带记忆评同一批 transcript"
- 重大里程碑前可考虑跨模型 (GPT/Gemini) 评同 rubric

## 关键设计决策 (已拍板)

| 决策 | 选择 | 理由 |
|---|---|---|
| L1 真 vLLM | 是 | 质量回归只能真 LLM 抓 |
| L2 走纯函数路径 | 是 | 避免改 spawn_engine 接口,LLM-dependent 留 L1 |
| **L1 砍机器断言** | 只留 files_exist + max_duration_s | judge 在语义层评得更准,机器断言对 LLM 创新方式过于刚性 |
| **Judge = Claude** | 跨模型 | 比 Qwen 自评有独立性 (本地零成本,不需 API key) |
| **Judge 跟 cargo test 解耦** | 是 | judge 漂移/换 rubric 不能阻塞 PR |
| CI 决策 | L2 进 CI / L1 本地手跑 + pre-release | vLLM 在 10.214.74.113 内网,GitHub Actions 进不来 |
| 健康探针 exit 0 | 是 | nightly 不告警 |

## Lesson learned (踩过的坑)

### reminder 改造单样本不可信
2026-05-18 尝试给 Plan/Planning system-reminder 加"禁过渡语 + text summary" 条款 (commit `7b983b6`),单 scenario 验证后 revert (commit `ec2f788`):
- 副作用确定: text 23→556 字暴增 (+24×) + system prompt 占位
- 改进不可信: 完整性 3→5 是单样本可能 LLM 抽奖
- 核心问题没解: LLM 仍写 5 次 "让我..." (mode-independent 行为)

**规则**: 改 reminder/INSTRUCTIONS_MD 前要 (a) 积累 3+ 次离群点证据 (b) 改后 2+ scenario A/B 验证。详 `process.md` `## L1 judge 离群点跟进` 下方 lesson learned。

### vLLM 配置变更可能"免费修"离群点
2026-05-18 GB10 vLLM 参数优化 (max-model-len 262144 / kv-cache fp8 / prefix-caching / chunked-prefill / max-num-seqs 8),baseline `v0.8.37-r1-13scn → vllm2-r2-17scn` 13 老 scenarios 子集:
- 总均分 4.67 → 4.77 (**+0.10**)
- `plan_travel_web` 自动修了 update_plan 离群点 (无代码改动,工具 3/5→5/5)
- `multi_turn_context` 不再用 code_execution 算简单数学 overkill (工具 4/5→5/5)
- `tool_error_recovery` 主动识别 test scenario (元认知改善)

**含义**: 离群点的根因可能在**底座配置而不是 prompt 工程**。改 prompt 前先看是否能调底座参数。
**警示**: 新 vLLM 配置同时**暴露了 subagent 链路问题** (3/4 subagent scenario SSE 失败),即免费红利和新瓶颈并存,需要全 scenario 评估而非只看 diff 表面均值。

## 待办 / 暂缓

- **GB10 self-hosted GitHub Actions runner**: L1 进 CI nightly,等团队 ≥2 人或发版频率上升再做
- **真 Tauri GUI Playwright**: 暂缓；Mock GUI smoke 已覆盖通用 UI 和知识库 UI
- **Scenario 提取到 toml**:当前 27 条仍共享较多 Rust fixture/helper，待场景数据结构稳定后再做
- **真 sanity 跨 session/跨模型**: 等重大里程碑前做

## 相关文档

- `docs/L1-judge-rubric.md` — Judge 评分 rubric + 操作流程
- `docs/真实用户自动化测试矩阵.md` — 用户旅程覆盖范围 + 一键入口
- `docs/l1-baselines/README.md` — Baseline 命名约定 + 已有 baseline 表
- `process.md` — 离群点跟进 + lesson learned
- `docs/验证报告-qwen3.6-deepseek-tui.md` — 历史手测基线
