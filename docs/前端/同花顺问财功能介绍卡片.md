import React, { useState, useEffect } from 'react';
import { Bot, Sparkles, MessageSquare, ArrowRight, Activity, PieChart, Newspaper, Globe, Sun, Moon } from 'lucide-react';

export default function App() {
  const [hoveredPrompt, setHoveredPrompt] = useState(null);
  const [isDarkMode, setIsDarkMode] = useState(false);

  // 模拟跟随系统主题，为了演示方便，这里直接提供手动切换
  const toggleTheme = () => setIsDarkMode(!isDarkMode);

  const prompts = [
    { text: "茅台最新股价", icon: <Activity className="w-4 h-4" /> },
    { text: "今天大盘怎么样", icon: <PieChart className="w-4 h-4" /> },
    { text: "市盈率低于10的银行股", icon: <Globe className="w-4 h-4" /> },
    { text: "最近降息新闻", icon: <Newspaper className="w-4 h-4" /> }
  ];

  return (
    <div className={`${isDarkMode ? 'dark' : ''} antialiased`}>
      <div className="min-h-screen bg-slate-50 dark:bg-slate-950 flex items-center justify-center p-4 font-sans transition-colors duration-500">
        
        {/* 核心卡片 */}
        <div className="max-w-md w-full bg-white dark:bg-slate-900 rounded-[2rem] shadow-2xl shadow-slate-200/50 dark:shadow-slate-900/50 overflow-hidden border border-slate-100 dark:border-slate-800/60 transition-all duration-500 relative">
          
          {/* 装饰性光晕 (仅在深色模式或特定状态下明显) */}
          <div className="absolute top-0 right-0 -mr-16 -mt-16 w-48 h-48 rounded-full bg-blue-500/10 dark:bg-blue-500/5 blur-3xl pointer-events-none"></div>

          {/* 头部状态区 */}
          <div className="relative bg-gradient-to-b from-blue-50/80 to-white dark:from-slate-800/50 dark:to-slate-900 p-6 border-b border-slate-100 dark:border-slate-800/60 flex items-center justify-between transition-colors duration-500">
            <div className="flex items-center space-x-3.5">
              <div className="bg-gradient-to-tr from-blue-600 to-indigo-500 p-2.5 rounded-xl shadow-lg shadow-blue-500/30">
                <Bot className="w-6 h-6 text-white" />
              </div>
              <div>
                <h2 className="text-[1.1rem] font-bold text-slate-800 dark:text-slate-100 tracking-tight transition-colors">同花顺问财</h2>
                <div className="flex items-center text-xs text-slate-500 dark:text-slate-400 mt-0.5 transition-colors">
                  <span className="relative flex h-2 w-2 mr-2">
                    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                    <span className="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
                  </span>
                  系统已就绪
                </div>
              </div>
            </div>

            {/* 深浅色模式切换按钮 */}
            <button 
              onClick={toggleTheme}
              className="p-2.5 rounded-full bg-slate-100/80 dark:bg-slate-800 text-slate-500 dark:text-slate-400 hover:text-blue-600 dark:hover:text-blue-400 hover:bg-white dark:hover:bg-slate-700 shadow-sm transition-all duration-300"
              title="切换主题"
            >
              {isDarkMode ? <Sun className="w-4 h-4" /> : <Moon className="w-4 h-4" />}
            </button>
          </div>

          {/* 主体介绍区 */}
          <div className="p-6 relative">
            <p className="text-slate-600 dark:text-slate-300 leading-relaxed text-[15px] transition-colors">
              12 个金融查询工具，覆盖<span className="font-semibold text-blue-600 dark:text-blue-400 mx-1">行情、财务、选股、宏观经济、新闻公告</span>。自然语言提问即可。
            </p>

            {/* 分割线 */}
            <div className="flex items-center my-7">
              <div className="flex-grow h-px bg-gradient-to-r from-transparent via-slate-200 dark:via-slate-700/50 to-transparent"></div>
              <span className="px-4 text-[11px] uppercase tracking-wider font-semibold text-slate-400 dark:text-slate-500 flex items-center space-x-1.5 transition-colors">
                <MessageSquare className="w-3.5 h-3.5" />
                <span>试试问我</span>
              </span>
              <div className="flex-grow h-px bg-gradient-to-r from-transparent via-slate-200 dark:via-slate-700/50 to-transparent"></div>
            </div>

            {/* 建议提示词按钮组 */}
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
              {prompts.map((prompt, index) => (
                <button
                  key={index}
                  onMouseEnter={() => setHoveredPrompt(index)}
                  onMouseLeave={() => setHoveredPrompt(null)}
                  className={`
                    group relative flex items-center justify-between p-3.5 rounded-2xl border text-left transition-all duration-300
                    ${hoveredPrompt === index 
                      ? 'border-blue-200/80 bg-blue-50/50 dark:border-blue-500/30 dark:bg-blue-500/10 shadow-sm' 
                      : 'border-slate-200/60 bg-slate-50/30 dark:border-slate-700/50 dark:bg-slate-800/30 hover:border-blue-200 dark:hover:border-slate-600'}
                  `}
                >
                  <div className="flex items-center space-x-3">
                    <div className={`
                      p-2 rounded-xl transition-colors duration-300
                      ${hoveredPrompt === index 
                        ? 'bg-blue-100/80 text-blue-600 dark:bg-blue-500/20 dark:text-blue-400' 
                        : 'bg-white text-slate-400 dark:bg-slate-800/80 dark:text-slate-500'}
                      shadow-sm
                    `}>
                      {prompt.icon}
                    </div>
                    <span className={`text-sm font-medium transition-colors duration-300 ${hoveredPrompt === index ? 'text-blue-700 dark:text-blue-300' : 'text-slate-700 dark:text-slate-300'}`}>
                      {prompt.text}
                    </span>
                  </div>
                  
                  <ArrowRight className={`
                    w-4 h-4 transition-all duration-300
                    ${hoveredPrompt === index ? 'text-blue-500 dark:text-blue-400 opacity-100 translate-x-0' : 'text-slate-300 dark:text-slate-600 opacity-0 -translate-x-2'}
                  `} />
                </button>
              ))}
            </div>
            
          </div>
        </div>
      </div>
    </div>
  );
}