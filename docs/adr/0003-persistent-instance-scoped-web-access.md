# 以实例级持久 Web 访问替代 Session 级手机远控

完整 WebUI 直接替代原有 Session 级临时手机远控，不提供旧协议兼容层。新的 Web 访问端点绑定整个桌面实例，一次启用后跨重启保持有效，用户主动停止或刷新凭证时才失效；同一端点首期只允许一个 Web 客户端操作，而桌面界面与 Web 客户端各自维护独立的导航和界面偏好。停止/刷新先持久记录旧端点撤销意图，Relay 离线或应用退出不会丢失，后续进程会重放到收到撤销 ACK。
