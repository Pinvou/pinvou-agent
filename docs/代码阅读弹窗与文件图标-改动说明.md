# 代码阅读弹窗与文件图标着色 — 改动说明

> 分支：`feat/code-viewer-popup`（基于 `feat/kimi-coding-optimization`）
> 方案文档：《代码阅读弹窗与文件图标-改动方案.md》
> 提交：
> - `4185ae43` feat(codex): 工作区文件点击改为可调大小的代码阅读弹窗
> - `0fedd252` feat(codex): 工作区文件树引入 VSCode 风格彩色文件图标

## 1. 本次改动了什么

### 1.1 代码阅读弹窗（提交一）

**用户可见行为**：在 Codex 会话右侧"工作区"面板点击任意文件，不再挤占面板内嵌区域，而是弹出居中的代码阅读弹窗：

- 代码按文件类型语法高亮（highlight.js，复用应用现有 markdown 代码块配色，明暗主题自动跟随）。
- 弹窗右侧、下侧、右下角均可拖拽调整大小（右下角双向），尺寸记忆在本地（localStorage），下次打开保持；双击右下角恢复默认尺寸。
- 头部 A−/A+ 按钮调整代码字号（10–24px，默认 12px，行高按 1.6 倍联动），同样经 localStorage（`pinvou_code_viewer_font_size`）记忆，重开保持。
- 文件树每行（非目录）悬浮提供外部打开按钮（`ExternalLink` 图标），与行点击的内嵌代码弹窗分工：行点击=内嵌看代码，行尾按钮=调系统关联应用打开（复用 `open_codex_workspace_file`，与弹窗头部「用系统应用打开」同一通路）。
- 头部提供：语言徽标、复制内容、复制路径、在文件管理器显示、用系统应用打开、关闭。
- 图片文件直接看图；超过 512KB 的文本显示"仅显示前一部分"提示条；不支持的二进制给出引导文案；读取失败显示错误态。
- Esc、点击遮罩、关闭按钮均可关闭；打开期间锁定背景滚动。

"变更"tab 的 diff 预览保留在面板内，行为不变。

**实现落点**：

| 文件 | 说明 |
|---|---|
| `pinvou3-app/src/features/codex/CodeViewerModal.jsx` | 新增。portal 模态弹窗；`highlightCode()` 高亮；三向拖拽 handle（rAF 节流）；localStorage `pinvou_code_viewer_size`（尺寸）与 `pinvou_code_viewer_font_size`（字号） |
| `pinvou3-app/src/features/codex/CodexWorkspacePanel.jsx` | 文件点击改开弹窗；`PreviewPane` 精简为 diff 专用；`openWorkspacePath()` 供弹窗与 diff 面板共用 |
| `pinvou3-app/src/shared/i18n.js` | `uiCodexWorkspace` 三语言（zh/en/ja）各新增 7 个文案 key |
| `pinvou3-app/src/styles/base.css` | `.code-viewer-body` 纳入既有 hljs 明暗主题变量作用域 |
| `pinvou3-app/tests/code_viewer_modal_smoke.mjs` 等 | 新增 puppeteer 冒烟测试（`npm run test:code-viewer-ui`） |

未新增 Rust 命令：文件内容沿用既有 `preview_codex_workspace_file`（文本 ≤512KB、图片 dataUrl）。

### 1.2 文件图标着色（提交二）

**用户可见行为**：工作区文件树与代码弹窗头部显示 VSCode 同风格（Material Icon Theme）的彩色图标——目录蓝黄文件夹（区分展开/收起），代码、配置、文档、图片、压缩包等按类型着色，`package.json`、`Cargo.toml`、`Dockerfile`、`.gitignore`、`README`、`LICENSE` 等有专属图标。

**实现落点**：

| 文件 | 说明 |
|---|---|
| `pinvou3-app/src/components/files/file-icon-theme.js` | 新增纯函数映射模块 `resolveFileIcon(name, {isDir, isOpen})`：特殊文件名 > 扩展名（约 90 个）> 默认 |
| `pinvou3-app/src/components/files/FileColoredIcon.jsx` | 新增图标组件，复用既有 `resolveAppAssetUrl` 资产解析（dev/build 均已验证） |
| `pinvou3-app/src/file-icons/theme/` | 43 个 SVG，取自 MIT 许可的 [Material Icon Theme](https://github.com/material-extensions/vscode-material-icon-theme)（VSCode 市场最流行的文件图标主题） |
| `THIRD_PARTY_NOTICES.md` | 补充该图标子集署名 |
| `pinvou3-app/tests/file_icon_theme.test.js` | 映射正确性 + 图标资产完整性断言，已纳入 `npm test` 主链路 |

## 2. 关于"是否需要移植 VSCode 核心"

**不需要，也不应该。** 结论与依据：

1. VSCode 的文件图标是**文件图标主题（File Icon Theme）**机制：一套 SVG/字体图标 + 一份「文件名/扩展名 → 图标」映射表，与编辑器内核（monaco）、workbench、主题服务完全解耦。VSCode 自带的 Seti 图标和市场上的 Material Icon Theme 都是独立扩展包。
2. 本次用约 1 天工作量、43 个 MIT 许可 SVG + 一个 100 余行的映射模块即达到 VSCode 同风格观感；移植 VSCode 核心则是月级工程，还会引入长期同步维护负担，对本需求没有收益。
3. 后续若要扩充图标覆盖面，只需从上游主题包追加 SVG 并在 `file-icon-theme.js` 补映射项即可，边际成本极低。

## 3. 验证记录（均实际执行）

| 验证项 | 结果 |
|---|---|
| `npm test`（pinvou3-app 全量主链路，含新图标测试） | 通过，无回归 |
| `node tests/code_viewer_modal_smoke.mjs`（本机 Edge 真实驱动） | 通过：弹窗渲染、高亮 token、语言徽标、截断条、三个拖拽 handle、默认 1100×760、拖拽 +30/+40 → 1130×800 且持久化、Esc 关闭、重开保持、双击恢复默认、字号默认 12px/行高 19px、A+/A− 调整并持久化、重开恢复上次字号 |
| `npx eslint`（全部改动文件） | 通过无告警 |
| `python3 scripts/architecture-guard.py` | 通过（no architecture debt increased） |
| `npm run build:ui` | 通过，43 个 SVG 正确拷贝进 dist |
| `test:ui-language` 三语言覆盖 | 通过 |

## 4. 验收清单

- [ ] 工作区点击 js/ts/rs/py/md/json 等文本文件 → 弹窗展示且高亮正确
- [ ] 三向拖拽调整大小；关闭后重开尺寸保持；双击右下角恢复默认
- [ ] A−/A+ 调整字号；关闭后重开字号保持
- [ ] 文件树文件行悬浮显示外部打开按钮，点击调系统应用打开；目录行不显示
- [ ] Esc / 遮罩 / 关闭按钮均可关闭弹窗
- [ ] 图片文件显示图片；大文本显示截断提示；二进制显示不支持提示
- [ ] "变更"tab 的 diff 预览行为与改版前一致
- [ ] 文件树与弹窗头部图标为彩色类型图标，目录展开/收起图标不同
- [ ] 中/英/日三种语言下弹窗文案正常

## 5. 已知边界与后续项

- 弹窗为只读查看，不支持编辑（设计内）。
- 文本高亮超过 128KB 自动回落纯文本（沿用 `syntax-highlighter.js` 既有保护），文件内容本身上限 512KB。
- 原面板内嵌预览底部的"添加到对话"未迁入弹窗；文件树每行悬浮 `+` 按钮仍可添加引用。
- 图标映射未覆盖的扩展名回落默认文件图标，可按需增量扩充。
- 冒烟测试覆盖文本场景；image/unsupported/error 分支走同一代码路径，未逐一做浏览器断言。
