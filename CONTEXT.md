# Pinvou 知识库

Pinvou 将用户资料组织为可挂载、可检索且能够追溯来源的知识。知识既可以由当前设备持有，也可以由独立服务持有。

## Language

**本地知识库**：
源文档和检索数据由运行 Pinvou 桌面应用的当前设备持有的知识库。
_Avoid_：客户端缓存、离线副本

**共享知识库**：
源文档和检索数据由共享知识库主机持有、可供多台 Pinvou 共同使用的知识空间。首期其中的多个知识集对所有已授权设备共同可见，不划分个人私有区。
_Avoid_：远程知识库、cube 知识库、共享 SQLite、网络磁盘知识库

**共享知识库主机**：
由本机 Pinvou 创建并持有共享知识库、供其他 Pinvou 设备通过网络使用的设备；首期每台主机只持有一个共享知识库服务。
_Avoid_：远程部署目标、固定 cube、云端账号

**创建共享知识库**：
将当前运行 Pinvou 的设备设为共享知识库主机；它不表示把服务远程安装到另一台设备。
_Avoid_：连接服务器、远程安装、SSH 部署

**持续共享**：
共享知识库不依赖 Pinvou 窗口保持打开；主机重启后恢复共享，直至服务器所有者明确停止。
_Avoid_：临时分享、保持应用打开、会话内服务

**共享服务升级**：
共享知识库主机上的 Pinvou 升级后，由用户在本机明确触发的后台服务升级；升级保留服务器身份、数据与授权，失败时恢复旧服务，不能由远程所有者执行。
_Avoid_：客户端升级、静默替换、远程升级

**发布到共享知识库**：
用户明确选择本地知识集并将其内容复制到共享知识库；发布后两份知识独立存在，不自动同步后续变化。
_Avoid_：自动共享、移动知识集、双向同步、镜像

**知识集**：
一组由用户命名并可挂载到会话中的知识文档。
_Avoid_：文件夹、数据库

**源文档**：
加入知识集的原始用户文件；远程知识库持有其受管副本，使知识可以脱离上传设备而存在。受管副本不跟随上传设备上的原路径自动变化，只通过用户的显式更新发生变化。
_Avoid_：索引文件、原路径

**文档更新**：
用新的源文档替换远程知识集内既有文档；只有新文档完整就绪后才成为当前版本，失败不会改变原有可用版本。首期不包含历史版本。
_Avoid_：自动同步、直接覆盖

**管理访问**：
允许检索并管理共享知识集的远程访问权限，包括上传、更新和删除。
_Avoid_：管理员账号、所有者用户

**只读访问**：
允许浏览、检索、查看来源和下载源文档，但不能上传、更新、删除或以其他方式改变共享知识集。
_Avoid_：访客账号、匿名访问

**分享连接**：
由共享知识库所有者生成、可供多台 Pinvou 在有效期内找到同一共享知识库并提交加入申请的连接；它不直接授予知识访问权限，默认 24 小时有效且可提前停止。
_Avoid_：一次性邀请、长期令牌、公开读取链接

**附近共享知识库**：
由 Pinvou 在当前局域网中自动发现、可供设备提交加入申请的共享知识库；附近发现不跨越 Tailscale 或其他路由网络，也不直接授予访问权限。
_Avoid_：Tailnet 扫描、自动授权、全网服务目录

**加入申请**：
一台 Pinvou 通过附近发现或分享连接请求获得共享知识库访问权的待处理记录；批准后该设备获得独立授权。
_Avoid_：用户注册、匿名访问、共享令牌

**自动批准只读**：
由服务器所有者为分享连接显式开启、在该连接有效期内将新的加入申请自动批准为只读访问的方式；默认不开启，关闭或到期后新申请恢复等待人工批准，既有设备授权不受影响。
_Avoid_：开发模式、内网只读、匿名访问、开放服务器

**共享知识库连接**：
一台 Pinvou 保存的某个共享知识库地址及其访问授权；同一台 Pinvou 可以连接多个共享知识库，知识集与来源始终归属于其中一个连接。
_Avoid_：远程连接、远程模式、全局服务器

**服务器身份**：
共享知识库在创建时取得、不会随地址或主机名称变化的稳定可验证身份；Pinvou 连接时以它确认自己访问的是同一共享知识库。
_Avoid_：IP 地址、显示名称、端口

**知识就绪**：
源文档已经由当前生效的 embedding 模型完成全文与语义索引，可以向 Pinvou 提供完整检索的状态。
_Avoid_：仅已上传、关键词降级可用

**所有者控制台**：
服务器所有者在 Pinvou 内创建和管理共享知识库的产品界面；共享知识库不提供浏览器管理后台。
_Avoid_：Web 后台、管理网站、浏览器控制台

**本机接管**：
共享知识库主机上的 Pinvou 经系统管理员确认后，重新取得已有共享知识库所有权的恢复操作；网络中的其他设备不能发起接管，正常升级也不会触发接管。
_Avoid_：初始化密钥、远程认领、首访者接管

**授权设备**：
获得远程访问授权、可由管理员命名并单独调整访问级别或撤销权限的一台 Pinvou 设备。
_Avoid_：用户账号、共享万能令牌

**所有者设备**：
已经加入共享知识库、再由共享知识库主机从成员列表中明确提升，可以在 Pinvou 中远程治理共享知识库并拥有完整内容管理能力的设备；它不能执行停服、移除、迁移、恢复或本机接管等主机生命周期操作，也不能取消主机的本机恢复能力。
_Avoid_：所有者邀请、普通管理访问设备、共享管理员密码、浏览器管理员

**服务器所有者**：
创建共享知识库并通过 Pinvou 持有服务器管理权的人，负责模型、存储、设备授权、备份和恢复；不等同于拥有管理访问的内容维护者。
_Avoid_：管理访问、普通管理员账号

**回收站**：
保存已从知识集删除、已立即退出检索但仍处于可恢复期限内的源文档或知识集。
_Avoid_：历史版本、备份、永久删除

**永久删除**：
由服务器所有者在回收站中确认执行、立即清除源文档、索引和数据库记录且不可恢复的操作。
_Avoid_：移入回收站、普通删除

**移除共享知识库**：
服务器所有者明确停止本机共享服务，并选择保留数据供以后恢复或永久删除全部共享数据的主机级操作；卸载 Pinvou 不属于移除共享知识库。
_Avoid_：卸载 Pinvou、关闭窗口、断开连接

**共享知识库备份**：
由所有者通过 Pinvou 创建、可用于恢复共享知识集及其源文档的完整可校验副本；模型可以重新准备，不属于备份内容。
_Avoid_：直接复制数据库、模型副本、回收站
